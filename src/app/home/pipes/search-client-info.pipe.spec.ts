import { SearchClientInfoPipe } from './search-client-info.pipe';

describe('SearchClientInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClientInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
